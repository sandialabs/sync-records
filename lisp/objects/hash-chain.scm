(define-class (hash-chain)

  (define (*init* self param-1 param-2))

  (define (get self) #t)

  (define (push! self) #t)

  (define (pop! self) #t)

  (define (head self) #t)

  (define (length self) #t))
