(define-class (chain-managerk)

  (define (*init* self length chain) #t)

  (define (get self index) #t)

  (define (resize! self length) #t))

  (define (push! self data) #t)

  (define (head self) #t)

  (define (length self) #t)
